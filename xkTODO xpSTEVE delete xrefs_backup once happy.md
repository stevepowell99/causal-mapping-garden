# Delete old xrefs cleanup backup

Backup of the 171 pre-cleanup source pages from the xrefs Related-block sweep is at `C:\tmp\xrefs_backup`. Delete it once you're happy the cleanup (stripped `<!-- xrefs-v1 -->` markers and trailing link-only `## Related` blocks, see `CLAUDE.md`) didn't lose anything.
