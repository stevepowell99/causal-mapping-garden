# Allowlist: only what Netlify needs (publish = dist). content/ stays local — never committed.
# build_static_site.py, config.yml, content/ are ignored by the * rule below.

*

!.gitignore
!netlify.toml
!dist/
!dist/**

# Build output copies of trash / local junk — never ship
desktop.ini
dist/.trash/
