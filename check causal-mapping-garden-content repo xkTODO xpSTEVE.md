Spotted 2026-08-04 while scoping Gabi's GitHub access: there is a separate **public** GitHub repo `causal-mapping-garden-content` (distinct from the `19aCMgarden` site repo, which holds only `dist/`, source `content/` is gitignored).

Check whether `causal-mapping-garden-content` is live or legacy. If legacy, it may be a stale public copy of the Garden notes — a dormant-and-still-reachable case for the standing "retire old stuff" task (see `reference_retirement_log` memory and hub `CLAUDE.md`).
